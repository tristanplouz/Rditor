#include "../class/Netconnection.h"


Netconnection::Netconnection(){
  //adress = "localhost";
  //port = 9876;
}

bool Netconnection::connect(){

  /*sf::TcpSocket socket;
  sf::Socket::Status status = socket.connect(adress, port);
  if (status != sf::Socket::Done)
  {
      return 0;
  }

  return 1;*/

}
